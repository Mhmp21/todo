{{-- @if (count($errors) > 0)
    <div role="alert">
        <div class="text-center flex flex-col justify-center w-1/3 mx-auto">
            <div class="rounded-md flex-col w-12/12 bg-red-500 mt-3 mb-4 border-2 ">
                @foreach ($errors->all() as $error)
                    <ul class="alert-text">{{ $error }}</ul>
                @endforeach
            </div>
        </div>
    </div>

@endif --}}
